backend:
  image:
    repository: ghcr.io/mikemin32/k8s-explorer-backend
    tag: v0.1.0
  replicaCount: 1

  serviceAccount:
    create: true
    name: k8s-explorer-backend

  nv:
    PROMETHEUS_URL: "http://kps-kube-prometheus-stack-prometheus.monitoring.svc:9090"

gateway:
  image:
    repository: ghcr.io/mikemin32/k8s-explorer-gateway
    tag: v0.1.0
  replicaCount: 1

ingress:
  enabled: true
  host: ""

rbac:
  create: true